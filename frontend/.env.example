# API Configuration (Render host is OK without /api/v1 — we append it in code)
VITE_API_URL=http://localhost:8000/api/v1

# When the backend has API_KEYS set, paste the same value here (never commit real keys).
# VITE_API_KEY=

# Environment
VITE_ENV=development
